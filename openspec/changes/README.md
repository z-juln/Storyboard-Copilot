# Changes

每个迭代一个短文件即可，文件名用 `kebab-case`。

```md
# <change name>

## Goal
- 用户要什么，1-3 条。

## Tasks
- [ ] 要改哪里或做什么。

## Acceptance
- 如何确认完成。
```

完成后：勾选 Tasks / Acceptance；稳定内容迁入 `openspec/<topic>/`（见 `.agents/skills/storyboard-openspec-sync/`）。可保留作归档，或发布后在 `docs/releases/` 吸收要点。
